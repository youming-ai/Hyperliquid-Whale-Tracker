{
  "name": "@hyperdash/contracts",
  "version": "1.0.0",
  "description": "tRPC router definitions and API contracts for HyperDash platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts,.tsx",
    "lint:fix": "eslint src --ext .ts,.tsx --fix"
  },
  "dependencies": {
    "@hyperdash/shared-types": "file:../shared-types",
    "@hyperdash/database": "file:../database",
    "@trpc/server": "^10.43.6",
    "@trpc/client": "^10.43.6",
    "zod": "^3.22.4",
    "superjson": "^2.2.1",
    "jose": "^5.1.3"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "typescript": "^5.3.3"
  }
}
