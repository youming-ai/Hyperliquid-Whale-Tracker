{
  "name": "@hyperdash/shared-types",
  "version": "1.0.0",
  "description": "Shared TypeScript types and utilities for HyperDash platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "lint": "biome check src",
    "lint:fix": "biome check --write src"
  },
  "dependencies": {
    "zod": "^3.22.4",
    "kafkajs": "^2.2.4",
    "avsc": "^5.7.7",
    "ws": "^8.14.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "@types/ws": "^8.5.10",
    "typescript": "^5.3.3"
  }
}
